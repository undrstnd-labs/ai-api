{
  "name": "@undrstnd/ai-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "api-dev": "pip3 install -r requirements.txt ; python3 -m uvicorn api.index:app --reload",
    "install": "pip3 install -r requirements.txt",
    "test:comp": "python3 tests/test_compilation.py",
    "dev": "pip3 install -r requirements.txt ; python3 -m uvicorn api.index:app --reload"
  },
  "dependencies": {
  }
}
